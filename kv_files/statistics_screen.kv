<StatisticsScreen>:
    on_enter:
        root.task()
    name: 'statistics_screen'
    Platime
    InfoLabel:
        text: "S T A T I S T I C S"
    #MDRoundFlatButton:
        #text: str(root.count_of_types) + "O przycisk"
        #pos_hint: {"center_x": .5, "center_y": .5}
        #size_hint: (0.25, 0.15)
        #on_release:
            #root.task()
    GridLayout:
        id: buttons
        cols: 2
        pos_hint: {"x": 0.05, "y": .1}
        size_hint: (0.9, 0.7)
    BoxLayout:
        orientation: 'horizontal'
        NavigatorButton:
            icon: 'clock-fast'
            on_release:
                root.manager.current = 'today_screen'

        NavigatorButton:
            icon: 'format-list-checks'
            on_release:
                root.manager.current = 'task_screen'
        NavigatorButton:
            icon: 'poll'
            on_release:
                root.manager.current = 'statistics_screen'